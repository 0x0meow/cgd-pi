# CoreGeek Displays Signage Player - Environment Configuration
# Reference: docs/server-api-events.md section 8.5
#
# Copy this file to .env and customize for your deployment:
#   cp .env.example .env
#   nano .env

# ============================================================================
# CoreGeek Displays Controller Configuration
# ============================================================================

# Base URL of your CoreGeek Displays controller instance
# This should be the publicly accessible HTTPS endpoint
# Example: https://displays.coregeek.com
CONTROLLER_BASE_URL=https://displays.example.com

# ============================================================================
# Venue Configuration
# ============================================================================

# Venue slug to display events for (leave empty for all public events)
# 
# Empty/commented: Fetches from /api/public/events (all venues)
# Specified: Fetches from /api/public/venues/:slug/events (single venue)
#
# Examples:
#   VENUE_SLUG=coregeek-taproom
#   VENUE_SLUG=downtown-location
#   VENUE_SLUG=
VENUE_SLUG=

# ============================================================================
# Data Refresh Configuration
# ============================================================================

# How often to fetch fresh event data from the controller (in seconds)
# Recommended: 60-300 seconds depending on event update frequency
# Lower values = more current data but higher network usage
FETCH_INTERVAL_S=60

# How often to rotate through event cards in the display (in seconds)
# Only applies when more than 3 events are displayed
# Recommended: 10-30 seconds for comfortable reading
DISPLAY_ROTATION_S=10

# Maximum number of events to display simultaneously
# Recommended: 6-12 depending on display resolution and card size
MAX_EVENTS_DISPLAY=6

# ============================================================================
# Offline Behavior
# ============================================================================

# How long to retain cached event data when controller is unreachable (in hours)
# After this period, the display will show "no events" if still offline
# Recommended: 24-72 hours for typical event planning cycles
OFFLINE_RETENTION_HOURS=24

# ============================================================================
# Server Configuration
# ============================================================================

# Internal HTTP port (accessed via localhost by Chromium)
# Do not change unless you have port conflicts
PORT=3000

# Node environment (production or development)
NODE_ENV=production

# ============================================================================
# Advanced Configuration (Optional)
# ============================================================================

# Custom signage URL override (for kiosk startup script)
# Leave commented to use default http://localhost:${PORT}
# SIGNAGE_URL=http://localhost:3000

# Logging level (error, warn, info, debug)
# LOG_LEVEL=info

# Enable detailed request logging (true/false)
# DEBUG_REQUESTS=false
